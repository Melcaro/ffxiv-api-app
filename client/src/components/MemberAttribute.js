import React from 'react';

export const MemberAttribute = ({ classJobs, minions, mounts }) => {
  console.log('classjobs', classJobs);
  console.log('minions', minions);
  console.log('mounts', mounts);
  return <div></div>;
};
